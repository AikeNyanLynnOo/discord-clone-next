export interface HomeFlexContent {
  img: string;
  title: string;
  text: string;
}

export interface NitroCardContent {
  textContent: string;
  imgSrc: string;
}

export interface NitroPlan {
  textContent: string;
  basic: string;
  special: string;
}
